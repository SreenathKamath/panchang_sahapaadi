# Copy to .env.production (or set directly in the Vercel project's Environment
# Variables UI) once the backend has a real URL. Leave unset for local dev -- Vite's
# dev proxy (vite.config.js) handles that instead.
VITE_API_BASE_URL=https://your-backend-domain.example.com
